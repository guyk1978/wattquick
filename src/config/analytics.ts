/**
 * Public GA4 measurement ID (visible in the browser once analytics loads).
 * Cloudflare Pages build env vars are not always available during prebuild,
 * so this is the source of truth for wattquick.com.
 */
export const WATTQUICK_GA_MEASUREMENT_ID = "G-MVWDC4SXZG";
